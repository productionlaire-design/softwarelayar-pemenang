name: pengumuman_pro
description: Aplikasi Pengumuman Pemenang
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  video_player: ^2.8.0
  window_manager: ^0.3.8

flutter:
  uses-material-design: true
